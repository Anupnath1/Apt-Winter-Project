{
  "name": "apt-security-scanner-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",

  "scripts": {
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "frontend": "vite --open",
    "backend": "cd ../backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "vite build",
    "preview": "vite preview"
  },

  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
